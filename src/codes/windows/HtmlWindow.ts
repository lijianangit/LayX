import BaseWindow from "./BaseWindow";
import Option from "../constraints/Option";

export default class HtmlWindow extends BaseWindow {
    constructor(options: Option) {
        super(options);
    }
}